{
  "name": "finance-roguelike",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check:python": "python -m py_compile finance_roguelike.py",
    "check": "node --check app.js && node --check src/persistence.js && node --check src/engine/run-config.js && npm run check:python && npm test"
  },
  "engines": {
    "node": ">=22"
  }
}
