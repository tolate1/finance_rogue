{
  "name": "finance-roguelike",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build --outDir dist/client && node scripts/prepare-sites-build.mjs",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --test",
    "check:python": "python -m py_compile finance_roguelike.py",
    "check": "node --check app.js && node --check src/persistence.js && node --check src/engine/run-config.js && node --check src/engine/economy.js && node --check src/engine/market-offers.js && node --check src/ui/turn-flow.js && npm run check:python && npm test"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "vite": "^8.1.4"
  },
  "dependencies": {
    "@fontsource-variable/pixelify-sans": "^5.3.0"
  }
}
